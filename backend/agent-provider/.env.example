# Service
PORT=8002
NODE_ENV=production
LOG_LEVEL=info
PUBLIC_BASE_URL=https://ai.overguild.com/agent-api

# Internal Python data layer (loopback only)
PYTHON_INTERNAL_URL=http://127.0.0.1:8001

# Postgres (reuses Signal's DB; must be reachable from Node)
DATABASE_URL=postgres://signal:signal@127.0.0.1:5432/signal

# ──────────────────────────────────────────────────────────────────────
# x402 payment rail — Arbitrum Sepolia testnet (standard `exact` scheme)
# USDC: 0x75faf114eafb1BDbe2F0316DF893fd58CE46AA4d (Circle, Arb Sepolia)
# Buyer pays gas (~$0.0001 testnet ETH) — no sponsor wallet, no local
# facilitator process. Facilitator default = public x402.org service.
# ──────────────────────────────────────────────────────────────────────
PAY_TO_ADDRESS=0xYourArbitrumSepoliaReceiver
FACILITATOR_URL=https://x402.org/facilitator

# ──────────────────────────────────────────────────────────────────────
# GOAT testnet rail (additive). When ENABLED=true, the 402 challenge
# advertises a 2nd accepts[] entry; buyer picks by paying on either
# chain. On retry, `x-payment-network` selects the rail for verify.
# Default token = WGBTC (0xbC10…0000) since USDC isn't issued on
# goat-testnet3. PAY_TO falls back to PAY_TO_ADDRESS above if not set.
# ──────────────────────────────────────────────────────────────────────
GOAT_X402_ENABLED=false
GOAT_X402_PAY_TO=
GOAT_X402_RPC_URL=https://rpc.testnet3.goat.network
GOAT_X402_TOKEN_ADDRESS=0xbC10000000000000000000000000000000000000
GOAT_X402_TOKEN_SYMBOL=WGBTC
GOAT_X402_TOKEN_USD_PRICE=65000
